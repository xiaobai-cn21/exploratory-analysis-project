# 潜在脏数据问题分析

基于对数据清洗脚本的深入分析，以下是**可能遗漏的脏数据问题**：

## 🔍 1. 数据一致性检查（评估表）

### 1.1 Level字段总和与tested_student_cnt不一致
**问题描述**：
- `tested_student_cnt` 应该等于所有 `level1_cnt` 到 `level7_cnt` 的总和
- 当前脚本**未检查**这个一致性约束

**影响**：
- 可能导致数据统计错误
- 影响后续分析结果的准确性

**检查方法**：
```python
# tested_student_cnt 应该等于 sum(level1_cnt + level2_cnt + ... + level7_cnt)
# 其中 '-' 应视为 0
```

### 1.2 Proficient_student_cnt与Level字段不一致
**问题描述**：
- **AP评估**：`proficient_student_cnt` 应该等于 `level3_cnt + level4_cnt + level5_cnt`
- **IB评估**：`proficient_student_cnt` 应该等于 `level4_cnt + level5_cnt + level6_cnt + level7_cnt`
- 当前脚本**未检查**这个业务逻辑约束

**影响**：
- 达标学生数统计可能不准确
- 影响AP/IB达标率的计算

## 🔍 2. 数值字段的合理性检查

### 2.1 负数检查
**问题描述**：
- Level字段（`level1_cnt` 到 `level7_cnt`）和 `proficient_student_cnt`、`tested_student_cnt` 不应该是负数
- `student_count`（课程表）不应该是负数
- 当前脚本**未检查**负数

**影响**：
- 负数在业务逻辑上不合理
- 可能导致统计计算错误

### 2.2 非整数检查
**问题描述**：
- 学生数量字段应该是整数，不应该是小数
- 当前脚本**未检查**是否为整数

**影响**：
- 虽然转换为数值时可能变成浮点数，但实际值应该是整数
- 小数可能表示数据录入错误

### 2.3 异常大的数值
**问题描述**：
- 虽然检查了范围约束，但可能还需要检查：
  - `tested_student_cnt` 是否异常大（例如超过学校实际学生数）
  - 各level字段的总和是否合理

## 🔍 3. 字段间的逻辑关系一致性

### 3.1 Aggregation_code与Aggregation_name的一致性
**问题描述**：
- 同一个 `aggregation_code` 应该对应相同的 `aggregation_name`
- 当前脚本**未检查**这种一致性

**影响**：
- 可能导致数据关联错误
- 影响按地区/学校分组统计的准确性

**检查方法**：
```python
# 同一aggregation_code应该有唯一的aggregation_name
df.groupby('aggregation_code')['aggregation_name'].nunique() > 1
```

### 3.2 SUBGROUP_CODE与SUBGROUP_NAME的一致性
**问题描述**：
- 同一个 `SUBGROUP_CODE` 应该对应相同的 `SUBGROUP_NAME`
- 当前脚本**未检查**这种一致性

**影响**：
- 可能导致学生群体分类错误
- 影响按学生群体统计的准确性

### 3.3 Aggregation_index与地理字段的逻辑关系
**问题描述**：
- 虽然检查了条件约束（哪些字段应该为空），但可能还需要检查：
  - `aggregation_index = 4` 时，`INST_ID` 是否在有效范围内
  - `COUNTY_CODE` 与 `aggregation_code` 的前几位是否一致
  - `NRC_CODE` 与 `aggregation_code` 的对应关系

## 🔍 4. 重复数据检查

### 4.1 完全重复的记录
**问题描述**：
- 可能存在所有字段都完全相同的重复记录
- 当前脚本**未检查**重复数据

**影响**：
- 重复数据会导致统计结果偏大
- 影响数据质量

**检查方法**：
```python
df[df.duplicated(keep=False)]
```

### 4.2 业务逻辑上的重复
**问题描述**：
- 可能存在业务逻辑上的重复（例如：同一学校、同一课程、同一学生群体、同一年级的多条记录）
- 需要根据业务规则判断是否应该存在多条记录

## 🔍 5. IB评估的特殊约束

### 5.1 IB评估的Level5-7字段
**问题描述**：
- IB评估中，`level5_cnt`、`level6_cnt`、`level7_cnt` 不应该全部为0或'-'
- 至少应该有一些IB评估有这些高级别分数
- 当前脚本**未检查**这个约束

**影响**：
- 如果所有IB评估都没有高级别分数，可能是数据质量问题
- 或者确实没有学生获得高分（需要业务确认）

### 5.2 IB评估的Level字段完整性
**问题描述**：
- IB评估应该使用 `level1` 到 `level7` 的所有级别
- 如果某些级别始终为0或'-'，可能需要检查数据录入是否正确

## 🔍 6. 数据格式问题

### 6.1 数值字段中的非数字字符
**问题描述**：
- 数值字段中可能包含除数字和'-'之外的其他字符
- 例如：空格、字母、特殊符号等
- 当前脚本在转换为数值时使用 `errors='coerce'`，会将无效值转为NaN，但**未报告**这些异常值

**影响**：
- 可能导致数据丢失（转为NaN后被过滤）
- 应该识别并报告这些异常值

### 6.2 字符串字段的格式问题
**问题描述**：
- 字符串字段可能包含：
  - 前后空格（已处理）
  - 特殊字符（如换行符、制表符等）
  - 编码问题（如乱码）

### 6.3 日期/时间字段格式
**问题描述**：
- `REPORT_SCHOOL_YEAR` 虽然检查了固定值，但可能还需要检查格式是否统一
- 例如：是否有 '2023-24'、'2023/24'、'2023_24' 等不同格式

## 🔍 7. 业务逻辑完整性检查

### 7.1 测试学生数小于5时的完整性
**问题描述**：
- 当 `tested_student_cnt < 5` 时，所有成绩字段应为'-'
- 当前脚本已检查，但可能还需要检查：
  - 是否所有相关字段都是'-'（包括 `proficient_student_cnt`）
  - 是否有部分字段是'-'而部分字段有数值的情况

### 7.2 课程表与评估表的关联性
**问题描述**：
- 如果两个表有相同的标识字段，可能需要检查：
  - 课程表中存在的学校/课程，在评估表中是否也有对应记录
  - 数据是否完整（不是必须，取决于业务需求）

## 🔍 8. 边界值和异常值

### 8.1 零值检查
**问题描述**：
- `tested_student_cnt = 0` 的记录是否合理？
- `student_count = 0` 的记录是否合理？
- 如果测试学生数为0，为什么会有这条记录？

### 8.2 异常大的值
**问题描述**：
- 虽然检查了范围约束，但可能还需要检查：
  - 值是否在合理范围内（例如：单个学校的测试学生数不应该超过该学校的学生总数）
  - 是否存在数据录入错误（例如：多输入了一个0）

## 🔍 9. 缺失值模式分析

### 9.1 系统性缺失
**问题描述**：
- 某些字段的组合可能导致系统性缺失
- 例如：特定地区、特定学校类型的数据是否完整

### 9.2 缺失值的合理性
**问题描述**：
- 某些字段的缺失值是否符合业务逻辑
- 例如：非必填字段的缺失是否合理

## 📋 建议的检查优先级

### 高优先级（必须检查）
1. ✅ **Level字段总和与tested_student_cnt的一致性** - 影响数据准确性
2. ✅ **Proficient_student_cnt与Level字段的一致性** - 影响达标率计算
3. ✅ **负数检查** - 业务逻辑上不合理
4. ✅ **完全重复的记录** - 影响统计结果

### 中优先级（建议检查）
5. ⚠️ **Aggregation_code与Aggregation_name的一致性** - 影响数据关联
6. ⚠️ **SUBGROUP_CODE与SUBGROUP_NAME的一致性** - 影响分类统计
7. ⚠️ **非整数检查** - 可能表示数据录入错误
8. ⚠️ **IB评估的Level5-7字段** - 业务逻辑完整性

### 低优先级（可选检查）
9. ℹ️ **数据格式问题** - 可能影响数据展示
10. ℹ️ **边界值和异常值** - 需要业务确认
11. ℹ️ **缺失值模式分析** - 用于数据质量报告

## 🛠️ 实施建议

1. **创建额外的数据质量检查脚本**（已完成：`src/additional_data_quality_checks.py`）
2. **在数据清洗流程中集成这些检查**
3. **生成详细的数据质量报告**，包括：
   - 发现的问题数量
   - 问题类型分布
   - 问题记录示例
   - 修复建议

## 📊 预期发现的问题

基于对类似教育数据集的了解，预期可能发现：
- **数据一致性问题**：5-10% 的记录可能存在level字段总和与tested_student_cnt不一致
- **重复数据**：0.1-1% 的记录可能是完全重复的
- **字段一致性**：少数aggregation_code可能对应多个aggregation_name
- **负数或异常值**：应该很少，但如果存在需要重点关注

---

**注意**：以上分析基于对数据清洗脚本的代码审查。实际的数据质量问题需要通过运行检查脚本来确认。

