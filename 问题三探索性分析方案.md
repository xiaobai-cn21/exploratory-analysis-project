# 问题三：考试达标率影响因素探索性分析方案（最终版）

> **核心目标**：识别并量化影响AP/IB考试达标率的关键因素，为预测模型提供特征重要性排序、关系强度、特征工程方向和数据质量评估。

---

## 一、分析目标与定位

### 1.1 核心目标

1. **理解目标变量**：达标率的分布特征和变异程度
2. **识别关键影响因素**：哪些特征影响最大？
3. **量化特征关系**：特征与达标率的相关性强度和重要性排序
4. **评估数据可用性**：数据质量对分析的影响

### 1.2 与问题一、二的差异化

| 维度                 | 问题一（教育公平性） | 问题二（地理分布）   | **问题三（成绩预测）**                     |
| -------------------- | -------------------- | -------------------- | ------------------------------------------------ |
| **分析目标**   | 揭示群体差异         | 识别资源配置问题     | **识别影响因素，量化关系**                 |
| **分析性质**   | 描述性分析（是什么） | 描述性分析（在哪里） | **关系性分析（为什么+如何影响）**          |
| **重点维度**   | 人口统计分组         | 地理空间分布         | **多维度特征关系**                         |
| **统计方法**   | 分组对比、Gap分析    | 地理聚合、覆盖率计算 | **相关性分析、特征重要性**                 |
| **可视化重点** | 分组条形图、箱线图   | 地图热力图、TreeMap  | **相关性热力图、散点图矩阵、特征重要性图** |
| **最终产出**   | 公平性问题识别       | 资源配置建议         | **特征候选清单、特征工程方向**             |

---

## 二、数据准备

### 2.1 数据源

- **主数据**：`assessment_by_level/by_level/AP_IB_Assessment_2024_level4_School_cleaned.csv`（学校级数据，228,774条记录）
- **辅助数据**：
  - `assessment_by_level/level_relationships/school_hierarchy.csv`（层级关系）
  - `assessment_by_level/by_level/AP_IB_Assessment_2024_level3_District_cleaned.csv`（学区级数据）

### 2.2 目标变量定义

**达标率计算**：

```python
# AP评估：达标 = level3 + level4 + level5
# IB评估：达标 = level4 + level5 + level6 + level7

def calc_proficiency(row):
    if row["APIB_IND"] == "AP":
        return row["level3_cnt"] + row["level4_cnt"] + row["level5_cnt"]
    else:  # IB
        return row["level4_cnt"] + row["level5_cnt"] + row["level6_cnt"] + row["level7_cnt"]

proficiency_rate = proficiency_cnt / tested_student_cnt
```

**数据有效性**：

- 只计算 `tested_student_cnt >= 5` 的记录（隐私保护）
- 抑制数据（`tested_student_cnt < 5`）标记但不用于率计算

### 2.3 特征分类与选择

#### 2.3.1 CODE vs NAME字段选择原则

| 字段对                         | CODE类型             | NAME类型         | **选择**                                                         | **理由**                               |
| ------------------------------ | -------------------- | ---------------- | ---------------------------------------------------------------------- | -------------------------------------------- |
| NRC_CODE ↔ NRC_DESC           | 数值（1-7，有序）    | 文本（7个类别）  | **NRC_CODE（数值特征）**`<br>`**NRC_DESC（可视化标签）** | CODE有序，可作为数值特征；NAME用于可视化标签 |
| COUNTY_CODE ↔ COUNTY_NAME     | 数值（1-68，标识符） | 文本（63个类别） | **COUNTY_NAME（分类特征）**                                      | CODE只是标识符，无顺序；NAME更易读，用于分类 |
| SUBGROUP_CODE ↔ SUBGROUP_NAME | 数值（1-29，标识符） | 文本（24个类别） | **SUBGROUP_NAME（分类特征）**                                    | CODE只是标识符，无顺序；NAME更易读，用于分类 |

**可视化时的处理**：

- **NRC**：使用 `NRC_DESC` 作为标签（如"NEW YORK CITY"），但分析时用 `NRC_CODE` 作为数值
- **COUNTY**：使用 `COUNTY_NAME` 作为标签和分类特征
- **SUBGROUP**：使用 `SUBGROUP_NAME` 作为标签和分类特征

#### 2.3.2 特征分类清单

**学生特征**（从SUBGROUP_NAME派生）：

- 经济状况：`is_economically_disadvantaged` (0/1)
- 种族：`is_black`, `is_hispanic`, `is_white`, `is_asian`, `is_multiracial`, `is_american_indian` (0/1)
- 性别：`is_female`, `is_male`, `is_nonbinary` (0/1)
- 教育类型：`has_disability` (0/1)
- 语言：`is_ell` (0/1)

**地区特征**：

- `COUNTY_NAME`（分类，63个类别）
- `NRC_CODE`（数值，1-7，有序）
- `NYC_IND`（二分类，0/1）

**资源特征**：

- `NRC_CODE`（数值，1-7）
- `district_avg_proficiency_rate`（数值，聚合特征）

**学科特征**：

- `SUBJECT_AREA`（分类，11个类别）
- `APIB_IND`（二分类，'AP'/'IB'）
- `GRADE_LEVEL`（有序分类，5个类别）

**不应作为预测特征**：

- `tested_student_cnt`（这是结果的一部分，不是预测因子）
- `aggregation_name`（学校名称，基数太高，过拟合风险）
- `COURSE_ID`/`COURSE_DESC`（142个课程，基数太高）
- `ITEM_DESC`（评估名称，基数太高）

---

## 三、分析阶段设计（12个图表）

### 阶段1：目标变量理解（1个图表）

#### 图表1：达标率分布与统计摘要

**目的**：了解目标变量的分布特征和基本统计量

**可视化方案**：

- **主图**：小提琴图（Violin Plot），按AP/IB分组
  - 显示分布形状、中位数、四分位数
  - 叠加箱线图显示关键分位数
- **统计卡片**：显示均值、中位数、标准差、样本量、最小值、最大值

**数据准备**：

```python
# 导出所有有效记录的原始达标率值
proficiency_rates = df_valid[df_valid["proficiency_rate"].notna()]["proficiency_rate"]

# 按AP/IB分组统计
stats_summary = df_valid.groupby("APIB_IND")["proficiency_rate"].agg([
    ("mean", "mean"),
    ("median", "median"),
    ("std", "std"),
    ("min", "min"),
    ("max", "max"),
    ("count", "count")
])
```

**如何阅读**：

- 分布形状：是否正态、是否有双峰、是否有异常值
- 中位数线：50%分位数位置
- 统计卡片：关键数值对比（AP vs IB）

**对预测的价值**：

- 判断是否需要数据变换（如log变换）
- 识别异常值范围
- 指导模型选择（回归 vs 分类）

---

### 阶段2：数据质量评估（1个图表）

#### 图表2：数据抑制率排序

**目的**：识别哪些子组/地区数据被抑制，评估数据可用性

**可视化方案**：

- **水平条形图**：按抑制率从高到低排序
- 显示Top 20-30个子组
- 颜色映射抑制率大小（红色=高抑制率，绿色=低抑制率）
- 按AP/IB分组

**数据准备**：

```python
# 按子组和AP/IB统计抑制率
suppression_data = df.groupby(["SUBGROUP_NAME", "APIB_IND"]).agg(
    total_records=("tested_student_cnt", "count"),
    suppressed_records=("tested_student_cnt", lambda x: (x < 5).sum())
).reset_index()

suppression_data["suppression_rate"] = (
    suppression_data["suppressed_records"] / suppression_data["total_records"]
)

# 按抑制率排序
suppression_sorted = suppression_data.sort_values("suppression_rate", ascending=False)
```

**如何阅读**：

- 条形长度 = 抑制率
- 排序 = 从高到低
- 关注高抑制率的子组（可能影响分析）

**对预测的价值**：

- 识别数据缺失模式
- 指导数据清洗策略
- 识别需要向上聚合的小样本子组

---

### 阶段3：学生特征影响分析（2个图表）

#### 图表3：人口统计分组达标率对比（按类别分组）

**目的**：识别哪些学生特征影响最大，量化与All Students的差距

**可视化方案**：

- **分组条形图**：按类别分组（经济状况、种族、性别、教育类型、语言等）
- 每个类别内按达标率排序
- All Students基准线（虚线）
- Gap标注：正Gap绿色，负Gap红色
- 标注最低达标率的子组

**SUBGROUP分类与细分规则**：

| 类别                | 子组数量 | 展示方式 | 说明                                                                                       |
| ------------------- | -------- | -------- | ------------------------------------------------------------------------------------------ |
| **经济状况**  | 2        | 全部展示 | Economically Disadvantaged vs Not Economically Disadvantaged                               |
| **种族/族裔** | 6        | 全部展示 | White, Black, Hispanic, Asian/Pacific Islander, American Indian/Alaska Native, Multiracial |
| **性别**      | 3        | 全部展示 | Female, Male, Nonbinary（注意Nonbinary样本可能很小）                                       |
| **教育类型**  | 2        | 全部展示 | General Education Students vs Students with Disabilities                                   |
| **语言**      | 2        | 全部展示 | Not English Language Learner vs English Language Learner                                   |
| **流动性**    | 2        | 全部展示 | Migrant vs Not Migrant                                                                     |
| **居住状况**  | 2        | 全部展示 | Homeless vs Not Homeless                                                                   |
| **寄养状况**  | 2        | 全部展示 | In Foster Care vs Not in Foster Care                                                       |
| **军属**      | 2        | 全部展示 | Parent in Armed Forces vs Parent Not in Armed Forces                                       |

**数据准备**：

```python
# 添加类别字段
def categorize_subgroup(name):
    if "Economically" in name:
        return "经济状况"
    elif name in ["White", "Black", "Hispanic", "Asian/Pacific Islander", 
                  "American Indian/Alaska Native", "Multiracial"]:
        return "种族/族裔"
    elif name in ["Male", "Female", "Nonbinary"]:
        return "性别"
    elif "Disabilities" in name or "General Education" in name:
        return "教育类型"
    elif "English Language" in name:
        return "语言"
    elif "Migrant" in name:
        return "流动性"
    elif "Homeless" in name:
        return "居住状况"
    elif "Foster Care" in name:
        return "寄养状况"
    elif "Armed Forces" in name:
        return "军属"
    else:
        return "其他"

demo_df["category"] = demo_df["SUBGROUP_NAME"].apply(categorize_subgroup)

# 计算与All Students的差距
all_students_rate = demo_df[demo_df["SUBGROUP_NAME"] == "All Students"]["proficiency_rate"].mean()
demo_df["gap_vs_all"] = demo_df["proficiency_rate"] - all_students_rate
```

**如何阅读**：

- 每个类别一组条形
- 条形高度 = 达标率
- 基准线 = All Students达标率
- Gap = 条形与基准线的距离
- 颜色 = Gap的正负（绿色正，红色负）

**结论示例**：

- 经济状况类别：经济困难59% vs 不困难78%（差距19个百分点）
- 种族类别：Black 47% vs Asian 79%（差距32个百分点）
- 残疾状态类别：残疾学生46% vs 普通教育71%（差距25个百分点）

**对预测的价值**：

- 识别最重要的分类特征
- 指导One-hot编码优先级
- 发现需要特别关注的弱势群体

---

#### 图表4：学生特征重要性排序

**目的**：量化各学生特征对达标率的影响强度

**可视化方案**：

- **水平条形图**：按重要性从高到低排序
- 显示Top 15-20个特征（所有24个子组）
- 标注重要性分数、样本量、p值
- 颜色区分AP/IB

**数据准备**：

```python
# 使用统计方法计算重要性
from scipy.stats import ttest_ind

importance_data = []
for subgroup in subgroups:
    if subgroup == "All Students":
        continue  # 跳过基准组
  
    subgroup_data = df[df["SUBGROUP_NAME"] == subgroup]["proficiency_rate"].dropna()
    all_students_data = df[df["SUBGROUP_NAME"] == "All Students"]["proficiency_rate"].dropna()
  
    if len(subgroup_data) > 0 and len(all_students_data) > 0:
        t_stat, p_value = ttest_ind(subgroup_data, all_students_data)
    
        # 计算效应量（Cohen's d）
        pooled_std = np.sqrt(((len(subgroup_data) - 1) * subgroup_data.std()**2 + 
                              (len(all_students_data) - 1) * all_students_data.std()**2) / 
                             (len(subgroup_data) + len(all_students_data) - 2)))
        cohens_d = (subgroup_data.mean() - all_students_data.mean()) / pooled_std
    
        # 重要性 = |Cohen's d| * -log10(p_value) * sqrt(sample_size)
        importance = abs(cohens_d) * (-np.log10(p_value + 1e-10)) * np.sqrt(len(subgroup_data))
    
        importance_data.append({
            "subgroup_name": subgroup,
            "importance": importance,
            "cohens_d": cohens_d,
            "p_value": p_value,
            "sample_size": len(subgroup_data)
        })

importance_df = pd.DataFrame(importance_data).sort_values("importance", ascending=False)
```

**如何阅读**：

- 条形长度 = 重要性分数
- 排序 = 从高到低
- 颜色区分AP/IB

**结论示例**：

- 经济状况重要性最高（36.09）
- 种族特征（Black, Hispanic, White）都很重要
- 这些特征应该优先纳入模型

**对预测的价值**：

- 特征选择：优先使用Top特征
- 特征工程：可以创建交互特征

---

### 阶段4：地区特征影响分析（2个图表）

#### 图表5：N/RC与达标率关系（含县级分布）

**目的**：量化资源能力与达标率的关系，识别N/RC内部差异

**可视化方案**：

- **主图**：散点图，X轴=N/RC代码（1-7），Y轴=达标率
  - 每个点是一个县
  - 按AP/IB分组（不同颜色）
  - 添加趋势线（线性回归）
- **辅助图**：每个N/RC显示该N/RC下所有县的分布（箱线图或小提琴图）
- **统计信息**：显示相关系数、每个N/RC的平均达标率

**数据准备**：

```python
# 按县和N/RC聚合
county_summary = df_county.groupby(["COUNTY_CODE", "COUNTY_NAME", "NRC_CODE", "APIB_IND"]).agg(
    tested_total=("tested_cnt_valid", "sum"),
    proficient_total=("proficiency_cnt_valid", "sum"),
).reset_index()

county_summary["proficiency_rate"] = (
    county_summary["proficient_total"] / county_summary["tested_total"].replace(0, np.nan)
)

# 修复超过100%的问题
county_summary = county_summary[county_summary["proficiency_rate"] <= 1.0]

# 按N/RC聚合（用于趋势线）
nrc_summary = county_summary.groupby(["NRC_CODE", "APIB_IND"]).agg(
    avg_proficiency_rate=("proficiency_rate", "mean"),
    county_count=("COUNTY_CODE", "count")
).reset_index()

# 计算相关系数
corr = county_summary["NRC_CODE"].corr(county_summary["proficiency_rate"])
```

**如何阅读**：

- 散点分布 = 每个县的达标率
- 趋势线 = N/RC与达标率的整体关系
- 箱线图 = 每个N/RC下县的分布范围
- 相关系数 = 关系强度

**结论示例**：

- N/RC与达标率正相关（相关系数0.386）
- LOW NEEDS（N/RC 6）地区达标率最高（80%+）
- CHARTER SCHOOL（N/RC 7）表现最差（39%）
- 同一N/RC下，县与县之间仍有差异

**对预测的价值**：

- N/RC是强预测因子
- 可以直接作为数值特征使用
- 可以创建N/RC的分组特征（如"高N/RC"、"低N/RC"）

---

#### 图表6：NYC vs 非NYC对比（按N/RC细化）

**目的**：识别NYC地理位置对达标率的影响，结合资源背景

**可视化方案**：

- **分组条形图**：按N/RC分组（1-7）
- 每个N/RC组内对比NYC和非NYC的达标率
- 按AP/IB分组
- 标注差异（Gap）

**数据准备**：

```python
# 按NYC_IND和NRC_CODE分组
nyc_nrc_summary = df_county.groupby(["NYC_IND", "NRC_CODE", "APIB_IND"]).agg(
    tested_total=("tested_cnt_valid", "sum"),
    proficient_total=("proficiency_cnt_valid", "sum"),
).reset_index()

nyc_nrc_summary["proficiency_rate"] = (
    nyc_nrc_summary["proficient_total"] / nyc_nrc_summary["tested_total"].replace(0, np.nan)
)

# 计算NYC vs 非NYC的差异
for nrc in [1, 2, 3, 4, 5, 6, 7]:
    nyc_data = nyc_nrc_summary[(nyc_nrc_summary["NRC_CODE"] == nrc) & (nyc_nrc_summary["NYC_IND"] == 1)]
    non_nyc_data = nyc_nrc_summary[(nyc_nrc_summary["NRC_CODE"] == nrc) & (nyc_nrc_summary["NYC_IND"] == 0)]
  
    if len(nyc_data) > 0 and len(non_nyc_data) > 0:
        gap = nyc_data["proficiency_rate"].mean() - non_nyc_data["proficiency_rate"].mean()
        # 记录差异
```

**如何阅读**：

- 按N/RC分组，每组内对比NYC和非NYC
- 观察不同N/RC下的NYC效应
- 注意：N/RC=1时，NYC_IND应该都是1，所以可能只有NYC数据

**结论示例**：

- N/RC=1（NYC类别）时，只有NYC数据，无法对比
- N/RC 2-7中，NYC和非NYC的差异因N/RC而异
- NYC效应与资源背景相关

**对预测的价值**：

- NYC标识是重要的地区特征
- 可以创建NYC×NRC的交互特征

---

### 阶段5：资源特征影响分析（1个图表）

#### 图表7：资源-结果象限图

**目的**：识别资源与表现的关系，发现异常案例

**可视化方案**：

- **散点图**：X轴=N/RC代码（资源指数），Y轴=达标率
- 按AP/IB分组（不同颜色）
- 按N/RC着色（不同形状）
- 添加象限分割线（中位数）
- 点的标签显示学校名（`aggregation_name`）

**数据准备**：

```python
# 学校级数据
resource_data = df_school[
    (df_school["aggregation_index"] == "4") &
    (df_school["SUBGROUP_NAME"] == "All Students") &
    (df_school["NRC_CODE"].notna()) &
    (df_school["proficiency_rate"].notna()) &
    (df_school["APIB_IND"].isin(["AP", "IB"]))
].copy()

# 确保包含所有N/RC（1-7）
# 点的text字段使用aggregation_name（学校名）
```

**如何阅读**：

- 四个象限：
  - 右上：高资源高表现（理想）
  - 右下：高资源低表现（异常，需关注）
  - 左上：低资源高表现（优秀案例）
  - 左下：低资源低表现（需要支持）
- 点的颜色 = N/RC分类
- 点的标签 = 学校名

**结论示例**：

- 大部分点集中在右上和左下象限
- 发现一些异常案例（高资源低表现，低资源高表现）

**对预测的价值**：

- 识别异常案例
- 发现资源与表现的不匹配

---

### 阶段6：学科特征影响分析（1个图表）

#### 图表8：学科达标率排序

**目的**：识别哪些学科更容易达标

**可视化方案**：

- **水平条形图**：按达标率从高到低排序
- 按AP/IB分组（不同颜色）
- 显示所有学科（11个）

**数据准备**：

```python
# 按学科和AP/IB聚合
subject_summary = df.groupby(["SUBJECT_AREA", "APIB_IND"]).agg(
    tested_total=("tested_cnt_valid", "sum"),
    proficient_total=("proficiency_cnt_valid", "sum"),
).reset_index()

subject_summary["proficiency_rate"] = (
    subject_summary["proficient_total"] / subject_summary["tested_total"].replace(0, np.nan)
)

# 按达标率排序
subject_summary = subject_summary.sort_values(["APIB_IND", "proficiency_rate"], ascending=[True, False])
```

**如何阅读**：

- 条形长度 = 达标率
- 排序 = 从高到低
- 对比AP和IB在同一学科的表现

**结论示例**：

- Other学科达标率最高（85%）
- ELA学科达标率最低（63%）
- AP和IB在同一学科表现不同

**对预测的价值**：

- 学科是重要的预测因子
- 可以使用One-hot编码

---

### 阶段7：综合特征分析（3个图表）

#### 图表9：特征-目标相关性（扩展）

**目的**：量化所有特征与达标率的相关性

**可视化方案**：

- **水平条形图**：按相关系数绝对值排序
- 显示Top 20个特征
- 颜色区分正负相关（蓝色=正，红色=负）

**数据准备**：

```python
# 数值特征：Pearson相关系数
numeric_features = ["NRC_CODE", "NYC_IND"]
correlations = []
for feature in numeric_features:
    corr = df_school[feature].corr(df_school["proficiency_rate"])
    correlations.append({
        "feature": feature,
        "correlation": corr,
        "abs_correlation": abs(corr),
        "type": "数值特征"
    })

# 分类特征：使用ANOVA eta-squared或Cramér's V
categorical_features = ["SUBJECT_AREA", "COUNTY_NAME", "APIB_IND", "GRADE_LEVEL"]
for feature in categorical_features:
    # 使用ANOVA计算eta-squared
    groups = [df_school[df_school[feature] == cat]["proficiency_rate"].dropna() 
              for cat in df_school[feature].unique()]
    if len(groups) > 1 and all(len(g) > 0 for g in groups):
        f_stat, p_value = f_oneway(*groups)
        eta_squared = f_stat / (f_stat + len(df_school) - len(groups))
        correlations.append({
            "feature": feature,
            "correlation": eta_squared,  # 使用eta-squared作为相关性度量
            "abs_correlation": eta_squared,
            "type": "分类特征"
        })

# 主要子组特征（从SUBGROUP_NAME派生）
subgroup_features = ["is_economically_disadvantaged", "is_black", "is_hispanic", 
                     "is_white", "is_asian", "is_female", "has_disability", "is_ell"]
for feature in subgroup_features:
    if feature in df_school.columns:
        corr = df_school[feature].corr(df_school["proficiency_rate"])
        correlations.append({
            "feature": feature,
            "correlation": corr,
            "abs_correlation": abs(corr),
            "type": "学生特征"
        })

correlations_df = pd.DataFrame(correlations).sort_values("abs_correlation", ascending=False)
```

**如何阅读**：

- 条形长度 = 相关系数绝对值
- 排序 = 从高到低
- 颜色 = 正相关（蓝色）vs 负相关（红色）

**结论示例**：

- N/RC_CODE相关性最强（0.386）
- NYC_IND负相关（-0.358，NYC表现较差）
- 学科特征相关性也很强

**对预测的价值**：

- 特征选择：优先使用强相关特征
- 特征工程：可以创建N/RC的分组特征

---

#### 图表10：特征重要性排序（统一方法）

**目的**：综合评估所有特征的重要性

**可视化方案**：

- **水平条形图**：按重要性排序
- 显示Top 15-20个特征
- 颜色区分特征类型（学生/地区/资源/学科）

**数据准备**：

```python
# 统一计算方法：
# 数值特征：|correlation|
# 分类特征：ANOVA eta-squared
# 统一归一化到0-1范围

importance_data = []

# 数值特征
for feature in numeric_features:
    corr = abs(df_school[feature].corr(df_school["proficiency_rate"]))
    importance_data.append({
        "feature": feature,
        "importance": corr,
        "method": "correlation",
        "type": "数值特征"
    })

# 分类特征
for feature in categorical_features:
    groups = [df_school[df_school[feature] == cat]["proficiency_rate"].dropna() 
              for cat in df_school[feature].unique()]
    if len(groups) > 1 and all(len(g) > 0 for g in groups):
        f_stat, p_value = f_oneway(*groups)
        eta_squared = f_stat / (f_stat + len(df_school) - len(groups))
        importance_data.append({
            "feature": feature,
            "importance": eta_squared,
            "method": "anova",
            "type": "分类特征"
        })

# 归一化到0-1范围
max_importance = max([item["importance"] for item in importance_data])
for item in importance_data:
    item["importance"] = item["importance"] / max_importance

importance_df = pd.DataFrame(importance_data).sort_values("importance", ascending=False)
```

**如何阅读**：

- 条形长度 = 重要性分数（归一化后）
- 排序 = 从高到低
- 颜色 = 特征类型

**结论示例**：

- SUBJECT_AREA重要性最高（43.30）
- COUNTY_NAME第二（29.10）
- NRC_CODE第三（0.386）

**对预测的价值**：

- 特征优先级：学科 > 县 > N/RC
- 编码策略：学科和县需要高质量编码

---

#### 图表11：特征组重要性对比（按业务含义分组）

**目的**：识别哪个特征组对预测最有价值

**可视化方案**：

- **条形图**：按特征组分组
- 显示每个特征组的平均重要性、最大重要性、特征数量
- 颜色区分特征组

**数据准备**：

```python
# 按业务含义分组
def categorize_feature(feature_name):
    if feature_name in ["is_economically_disadvantaged", "is_black", "is_hispanic", 
                        "is_white", "is_asian", "is_female", "has_disability", "is_ell"]:
        return "学生特征"
    elif feature_name in ["COUNTY_NAME", "NYC_IND"]:
        return "地区特征"
    elif feature_name in ["NRC_CODE", "district_avg_proficiency_rate"]:
        return "资源特征"
    elif feature_name in ["SUBJECT_AREA", "APIB_IND", "GRADE_LEVEL"]:
        return "学科特征"
    else:
        return "其他"

importance_df["feature_group"] = importance_df["feature"].apply(categorize_feature)

group_importance = importance_df.groupby("feature_group").agg(
    avg_importance=("importance", "mean"),
    max_importance=("importance", "max"),
    feature_count=("feature", "count")
).reset_index()
```

**如何阅读**：

- 条形高度 = 平均重要性
- 对比不同特征组
- 观察哪个特征组最重要

**结论示例**：

- 学科特征组平均重要性最高
- 学生特征组第二
- 资源特征组第三

**对预测的价值**：

- 特征工程方向：重点关注重要性高的特征组
- 模型设计：可以为不同特征组设计不同的处理方式

---

## 四、关键修复点总结

### 4.1 图表调整

1. ✅ **删除图表5**（组合分析，数据限制无法计算真正的交互效应）
2. ✅ **合并图表6和7**为一个综合图表（N/RC与达标率关系，含县级分布）
3. ✅ **保留图表8**（NYC vs 非NYC对比，不排除N/RC=1）
4. ✅ **图表总数**：从13个减少到12个

### 4.2 特征选择明确化

1. ✅ **NRC**：使用 `NRC_CODE` 作为数值特征（有序），`NRC_DESC` 作为可视化标签
2. ✅ **COUNTY**：使用 `COUNTY_NAME` 作为分类特征和可视化标签
3. ✅ **SUBGROUP**：使用 `SUBGROUP_NAME` 作为分类特征和可视化标签
4. ✅ **不重复分析**：CODE和NAME不分别分析，只选择其中一个

### 4.3 SUBGROUP分类与细分

1. ✅ **图表3**：按类别分组展示，每个类别内对比子组
   - 多子组类别（如种族，6个子组）全部展示
   - 二分类别（如经济状况，2个子组）只对比两个子组
2. ✅ **图表4**：不按类别分组，直接按重要性排序所有24个子组

### 4.4 数据修复

1. ✅ 修复资源象限图数据（包含所有N/RC和AP/IB，修复标签）
2. ✅ 修复县级排名（超过100%问题，按N/RC分组）
3. ✅ 扩展相关性分析（包含更多特征，移除tested_student_cnt）

### 4.5 可视化改进

1. ✅ 达标率分布：小提琴图+统计卡片
2. ✅ 抑制率：排序条形图
3. ✅ 人口统计：分组条形图+基准线+Gap标注
4. ✅ 学科对比：排序条形图
5. ✅ 特征组重要性：按业务含义分组

### 4.6 方法改进

1. ✅ 重要性计算：使用真正的统计方法（t-test, ANOVA, Cohen's d）
2. ✅ 相关性计算：包含分类特征（使用ANOVA eta-squared）
3. ✅ 特征分组：按业务含义分组（学生/地区/资源/学科）

---

## 五、预期产出

### 5.1 数据文件（约15个）

- 所有分析结果的CSV和JSON格式
- 图表规格文件（`chart_specs.json`）

### 5.2 可视化图表（12个）

- 所有图表在前端页面中展示
- 使用Plotly交互式可视化

### 5.3 分析报告

- 关键发现总结
- 特征重要性排序
- 特征工程建议
- 建模指导

---

## 六、对预测模型的价值

### 6.1 特征选择

- 识别Top 15-20个最重要特征
- 提供特征优先级排序

### 6.2 特征工程

- **交互特征**：经济状况×种族、NYC×NRC
- **聚合特征**：学区平均达标率
- **派生特征**：从SUBGROUP_NAME创建二分类特征（is_black, is_economically_disadvantaged等）
- **编码建议**：One-hot（学科、县），Target Encoding（高基数分类特征）

### 6.3 模型选择

- 基于特征类型选择合适模型
- 基于目标变量分布选择回归或分类

---

## 七、实施计划

### 7.1 数据准备阶段

1. 加载和预处理数据
2. 计算达标率
3. 创建派生特征（从SUBGROUP_NAME创建二分类特征）
4. 处理缺失值和抑制数据

### 7.2 分析实施阶段

1. 按阶段顺序实施12个图表的分析
2. 确保数据质量和一致性
3. 导出所有结果文件

### 7.3 前端可视化阶段

1. 更新前端页面（删除图表5，合并图表6+7）
2. 更新JavaScript渲染逻辑
3. 测试所有图表显示

---

**本方案解决了之前发现的所有问题，每个图表都有明确的目的、正确的数据准备方法和合理的可视化方案。**
